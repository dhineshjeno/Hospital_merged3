// src/repositories/ehrRepository.js
// EHR Repository — placeholder module.
// The EHR controller delegates to consultationRepository, vitalRepository,
// and diagnosisRepository directly. This file exists so the require() in
// ehrController.js resolves without error.

module.exports = {};
